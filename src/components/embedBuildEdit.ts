// 임베드 템플릿 수정용 컴포넌트

type EmbedEditProps = {
    title_id?: string;
    description_id?: string;
    color_id?: string;
    footer_id?: string;
    image_id?: string;
    thumbnail_id?: string;
    author_id?: string;
    field_id?: string;
};

export const embedEdit = async (embed_id: string, embedEditProps: EmbedEditProps) => {
    //
};
